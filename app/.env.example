# ---- Backend URL (leave empty in dev — Vite proxy handles it) ----
# In production (Vercel), set this to your Railway backend URL:
# Example: VITE_BACKEND_URL=https://your-backend.railway.app
VITE_BACKEND_URL=
